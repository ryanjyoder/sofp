#!/usr/bin/env bash

## Get ENVs ###
export SIMULTANEOUS_PARSE="$(snapctl get simultaneous-parse)"
storage_dir="$(snapctl get storage-dir)"
if [ -z "$storage_dir" ]
then
      echo "storage-dir must be set"
      exit 1
fi

export PATH=$SNAP/bin:$PATH

### Start daemon ###
$SNAP/bin/sqlitebuilder $storage_dir
